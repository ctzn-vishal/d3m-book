import { DropCap } from '@/components/Book/DropCap';
import { QuizBlock } from '@/components/Book/QuizBlock';
import { DataTable } from '@/components/Book/DataTable';
import { Step } from '@/components/Book/Step';
import { KeyNumber } from '@/components/Book/KeyNumber';
import { Callout } from '@/components/Book/Callout';
import { Pitfall } from '@/components/Book/Pitfall';
import { Takeaway } from '@/components/Book/Takeaway';
import { LadderPosition } from '@/components/Book/part0/Part0Diagrams';
import {
  DashboardArc,
  KpiTile,
  TrendLine,
  RegionBreakdown,
  DaypartDrilldown,
  RecommendedAction,
  BuffetVsMemo,
} from '@/components/Book/part2/DashboardArc';
import questionMapRaw from './data/question-chart-map.json';

export const questionMap = questionMapRaw.dataPoints;

<div className="not-prose mb-6">
  <LadderPosition compact current={1} caption="Part II — visual evidence makes patterns visible before any method is named." />
</div>

<DropCap>

The CEO opens the new Bean & Basket executive dashboard. Twelve tiles, six charts, three filter dropdowns, two KPI rings, and a heatmap. Each one looks polished. After ninety seconds of scanning, he closes the tab. He could not have answered, from the dashboard, why revenue was flat last quarter — not because the answer wasn't in there, but because nothing on the page told him where to look first. The dashboard had charts. It did not have a question. A good dashboard is the opposite: every panel answers one specific question, and the panels are ordered so that the answer to the first leads naturally to the second, which leads to the third. The CEO's eye should never have to wander. The page should walk him through the analysis.

</DropCap>

## The executive question: *what should a manager notice first?*

A dashboard is a *sequence of business questions*. Not a collection of charts, not a status report, not a data buffet — a sequence. The first panel asks an executive question, the second answers it, the third asks the natural follow-up, and so on until the page ends at a recommended action. When a manager looks at a well-built dashboard, she does not scan; she reads, in the order the page is laid out, and the order makes sense.

The instinct to "show everything" is the most common mistake in dashboard design. Twelve tiles, each showing a different KPI, each presented as if equally important, force the viewer to do the prioritization work that the dashboard was supposed to do for her. The defense is to design the page like an analyst writes a memo: start with the question, state the headline number, show the trend, break it down where the trend gets interesting, and end with what to do about it. The same five-step arc, every page.

Before getting to the arc, the smaller question: *which chart for which question?* Figure 1 is a working table. It is not exhaustive — every analytics team has its own variants — but it is the minimum vocabulary a manager should expect a dashboard designer to know. The right chart is determined by the *question*, not by what the data team happens to have a template for.

<DataTable
  caption="Figure 1. Ten common executive questions and the chart that answers each. The order on this page does not matter; the order on a real dashboard does. Pick the chart from the question, not the other way around — and resist the urge to add a chart because the template was available."
  columns={[
    { key: 'question', label: 'Business question', align: 'left' },
    { key: 'chart',    label: 'Chart',             align: 'left' },
    { key: 'use_when', label: 'Use when',          align: 'left' },
  ]}
  rows={questionMap}
/>

Figure 1 names ten question-chart pairs. The pattern across all ten is that the *question shape* determines the *chart shape*: continuous time becomes a line; a fixed sequence becomes a funnel; categorical comparison becomes a sorted bar. There is no chart in Figure 1 whose primary job is decoration. Every entry exists because some specific kind of business question is hard to answer without that visual.

A dashboard is one or more of these charts arranged in an order. The order is the part most teams get wrong. Below is the order that has worked for fifty years and will work for fifty more: the executive question on top, the KPI tile that lands the headline, the trend that puts the headline in context, the breakdowns that explain the trend, and the recommended action at the bottom.

## The dashboard arc, in six steps

<Step number={1} title="The executive question (one sentence at the top)">

Every dashboard starts with a question, written in plain English at the top of the page. Not *"Sales overview."* Not *"Q1 performance."* Something specific: *"Why did revenue flatten in Q1, after eight quarters of growth?"* The question names what the rest of the page is trying to answer, and gives the reader a frame for the numbers that follow.

</Step>

<Step number={2} title="The KPI tile — the headline number">

One number, large, with a comparison. Last quarter's revenue against the quarter before, or against the same quarter last year. The KPI tile is the dashboard's headline. Used well, it gives a viewer the answer to the executive question in about two seconds.

<KeyNumber
  value="$1.18M"
  unit="Q1"
  label="Bean & Basket chain-wide revenue, Q1 2024."
  change="+0.7% vs Q4 2023"
/>

<div className="not-prose my-4">
  <KpiTile />
</div>

</Step>

<Step number={3} title="The trend — put the headline in context">

A line chart of the same metric over the last 8–12 periods. Without the trend, "+0.7%" reads as steady; with the trend, it reads as the first non-growth quarter after a year of 4–6% gains. The chart in this position is almost always a line chart, almost always with the most recent point flagged, and almost always *bare* — no clutter, no second metric, no overlaid bar chart. The trend's whole job is to give context for the KPI above.

<div className="not-prose my-4">
  <TrendLine />
</div>

</Step>

<Step number={4} title="The breakdown — where is the change coming from?">

Once the trend establishes that something interesting happened, the next panel decomposes it. Revenue by region. Revenue by product category. Revenue by store cohort. The right breakdown is the one whose variance explains the trend — usually two or three breakdowns are tried, and the one where the differences are largest stays on the dashboard. The chart is almost always a sorted bar, sometimes a small-multiples grid if there are too many groups.

<div className="not-prose my-4">
  <RegionBreakdown />
</div>

</Step>

<Step number={5} title="The drilldown — segment to the actionable level">

The breakdown identifies the *where*; the drilldown finds the *what*. If the regional breakdown surfaces Suburban as the breakout (Chapter 2's lesson made concrete), the drilldown digs into Suburban: customer segments, hour-of-day patterns, product mix. This is where exploratory visualization lives — scatterplots, small multiples, anything that admits *new information* into the analysis. Most viewers do not need this panel every day. Analysts do.

<div className="not-prose my-4">
  <DaypartDrilldown />
</div>

</Step>

<Step number={6} title="The recommended action">

The last panel is the only one that is not a chart. It is a paragraph — sometimes two — that translates the four chart panels into a recommendation. *"Suburban grew on weekday-morning commuter traffic. Recommend doubling weekday-morning staffing and piloting a 7am promo at the Campus store, where commuter foot traffic is comparable but conversion lags by 11 points."* This is the panel most dashboards omit. It is also the only panel that turns the dashboard from a status report into a decision document.

<div className="not-prose my-4">
  <RecommendedAction />
</div>

</Step>

Here is the whole arc on one page — the executive question on top, then the five panels reading straight down to the recommended action. Hover any chart for the underlying numbers. Notice that each panel answers the question the panel above it raises.

<DashboardArc />

The six steps above are not specific to a particular tool. They work in Looker, in Tableau, in a printed PDF, in a one-pager taped to a wall. What makes the dashboard work is not the platform; it is the *sequence*. Without a sequence, the same six panels are a buffet; with a sequence, they are a memo.

<Callout variant="finding" title="Live dashboard example">
The [Presidential Election Atlas](/studios/presidential-election-atlas) is a compact politics example of this arc. The page starts with the election result, puts the state map in the diagnostic position, then uses state swings and battleground margins to decide what a reader should inspect next.
</Callout>

<Pitfall title="The buffet dashboard">
Twelve tiles, no order, no hierarchy — every chart the same size, every metric presented as if equally important — forces the viewer to do the prioritization the dashboard was supposed to do. **Pick one executive question per page; if there are three questions, build three pages.**
</Pitfall>

The difference is entirely in the ordering. Toggle below between the *buffet* (the same four panels as equal tiles, no obvious place to start) and the *memo* (the arc). The data never changes — only whether the page does the prioritizing for you.

<BuffetVsMemo />

<Pitfall title="Vanity metrics on top">
Total page views, total accounts, total impressions — large, growing, and connected to no decision. **A KPI tile should answer a question the manager would otherwise ask; a number nobody would ever act on does not belong on the headline row.**
</Pitfall>

<Pitfall title="Charts without comparisons">
A single bar showing "Q1 revenue: $1.18M" is not a chart, it is a label. **Every chart on a dashboard should make a comparison** — to last period, to plan, to target, to a benchmark. "$1.18M, +0.7% vs Q4" is informative; "$1.18M" alone is not.
</Pitfall>

The deeper pathology behind all three is the same: building dashboards from the *data the team has* instead of the *questions the manager has*. The fix is to start every dashboard project with the question, written down, before any chart template is opened. A team that does this has dashboards that survive their first review. A team that doesn't builds beautiful tools that get closed after ninety seconds.

<QuizBlock
  title="Concept check"
  intro="These three questions span choosing a chart from the business question, looking at a full distribution before trusting one summary number, and the difference between exploring data and presenting an executive answer."
  questions={[
    {
      prompt: <>A regional VP asks: "which of our twelve metro markets grew fastest in home values since 2020?" Your housing dataset has monthly price levels per market, but the markets start at very different price points, so a raw multi-line chart is dominated by the expensive coastal markets sitting at the top. Which chart best answers the VP's actual question?</>,
      options: [
        { label: <>An indexed line chart that rebases every market to 100 at the start of 2020, so each line shows percent growth and the fastest grower is the steepest line regardless of starting price.</> },
        { label: <>A raw multi-line chart of price levels, because it shows the true dollar value of each market and the VP can read growth from how high each line ends up.</> },
        { label: <>A heatmap of market by month colored by price level, because it fits all twelve markets in one panel without overlapping lines.</> },
      ],
      correctIndex: 0,
      explanation: <>The question is about relative growth, so the comparison must be growth, not level. Indexing each market to 100 at a common start converts the lines to percent change, making the fastest grower the steepest line no matter where it started. The raw level chart is the tempting trap: a market can end up high simply because it began high, so "ends up highest" answers "which is most expensive," not "which grew fastest." The heatmap colored by level has the same flaw and buries the ranking the VP needs in a wall of color. The comparison determines the chart, not the other way around.</>,
    },
    {
      prompt: <>Across all Progresso store-months, average monthly unit volume is about 900 units, and the finance team proposes staffing and inventory plans assuming a typical store-month sells around 900. Before signing off, you plot the full distribution of store-month volumes. Which finding would most change how you act on that 900 figure?</>,
      options: [
        { label: <>The distribution is tightly clustered just above and below 900, confirming that nearly every store-month behaves like the average.</> },
        { label: <>The distribution is heavily right-skewed: most store-months sell well under 900, while a thin tail of very high-volume store-months pulls the mean up, so the mean describes almost no actual store.</> },
        { label: <>The distribution is centered exactly on 900 but the chart looks busy, which mainly tells you the histogram needs fewer bins.</> },
      ],
      correctIndex: 1,
      explanation: <>This is the look-before-you-summarize lesson. A right-skewed shape means a few high-volume store-months drag the mean above where the bulk of stores actually sit, so planning every store around 900 over-resources the typical low-volume store and under-resources the rare giants. The single mean hides that shape entirely; only the distribution reveals it. The tight-cluster option would justify trusting the mean, but it is contradicted by the known skew in the soup store-month data. The third option mistakes a real signal for a cosmetic binning issue and dodges the business consequence.</>,
    },
    {
      prompt: <>You are deep in the drilldown panel of a dashboard, where the article says exploratory visualization lives. You build a scatterplot of store foot traffic against conversion and notice three odd stores far from the cloud of points. A colleague says, "great, drop those three outliers straight onto the executive KPI row so leadership sees them." What is the soundest response?</>,
      options: [
        { label: <>Agree, since the scatterplot already revealed the outliers, the exploratory work is done and the same chart can headline the executive page.</> },
        { label: <>Push back: the scatterplot is exploratory, meant to admit new questions, so the next step is to investigate why those three stores differ, not to promote a raw exploratory chart into the headline that is supposed to deliver a settled, comparison-driven answer.</> },
        { label: <>Replace the scatterplot with a single KPI tile reading "3 outlier stores" and put that on the headline row, because one number is always cleaner than a chart for executives.</> },
      ],
      correctIndex: 1,
      explanation: <>The article places scatterplots and small multiples in the drilldown, where the job is to admit new information and raise questions, which is the essence of exploratory work. Finding three unusual stores is the start of an inquiry, not a finished result, so the disciplined move is to ask why they differ before anything reaches leadership. The headline row, by contrast, is confirmatory in spirit: it lands a settled comparison the manager can act on in seconds. Promoting a raw exploratory scatterplot there skips the investigation. The third option is the vanity-metric trap the article warns about: "3 outlier stores" is a number connected to no decision, so a clean tile does not make it belong on the headline.</>,
    },
  ]}
/>

<Takeaway label="The operating posture">

Before asking *which chart should we make?*, ask *which question are we trying to answer?* Then ask, *what is the next question that will naturally come up after this one is answered?* That second question is what determines the second panel. Working through the sequence — question, headline, context, breakdown, drilldown, action — produces dashboards that read top to bottom and end somewhere useful. *The discipline is editorial, not technical: the charts are easy; the order is the work.* A page that walks an executive through the analysis is a page the executive will open. A page that asks the executive to do their own prioritization is a page that quietly disappears from their bookmarks.

</Takeaway>
