# Survey-analysis prompt for §6 Social Trust

Use this with the `python-survey-analysis` Personal skill (post-restart). Paste verbatim:

---

Compute weighted population shares (with design-based 95% CIs) for two GSS items, cross-tabulated by income quintile and political party, pooled across all available years.

**Goal:** Produce the proposal's punchline statistics for *The Great Sorting* §6 — Income × Party interaction effects on (a) generalized social trust and (b) meritocracy belief.

**Outcomes (binary, derived from the categorical questions):**
1. `trust_high = 1 if trust == "Most people can be trusted" else 0` (from `trust_rec` or `trust`)
2. `getahead_high = 1 if getahead == "Hard work" else 0` (from `getahead_rec` or `getahead`)

**Strata:** income quintile × party. Use whatever income coding exists in the parquet. If `realinc_quintile` exists, use it; otherwise bucket from raw `realinc` / `income` / `coninc` into Bottom 20% / 21–40% / 41–60% / 61–80% / Top 20%. Document the variable + bucketing you actually used. Party should be Democrat / Independent / Republican.

**Data location:** GSS microdata at `gss/raw-data/gss_data_all.parquet` in Tigris. Tigris credentials in `/Users/umahuggins/github/ctzn-pub/.claude/worktrees/priceless-gauss-042ceb/.env`:
- TIGRIS_ENDPOINT=https://t3.storage.dev
- TIGRIS_BUCKET_NAME=ontopic-data
- TIGRIS_CLIENT_ID, TIGRIS_CLIENT_SECRET (in .env)

**Survey design:** Use `weight_use` for sampling weights, `vstrat` for stratification, `vpsu` for primary sampling units. Use `svy` (svyLab) for the design object — Taylor-series linearization for design-based standard errors.

**Pooling:** Pool across all years where outcome, income, party, and design variables are non-missing. Document years pooled and any years dropped.

**Sample restriction:** Standard GSS analytic sample. Surface cells with unweighted n < 30 with a `below_min_n: true` flag rather than dropping silently.

**Output:** Save TWO JSON files to `/Users/umahuggins/github/ctzn-pub/.claude/worktrees/priceless-gauss-042ceb/app/book/ch06-social-trust/data/`:

1. `trust-by-income-party-pooled.json`
2. `getahead-by-income-party-pooled.json`

**JSON schema** for each:
```json
{
  "metadata": {
    "outcome": "trust_high",
    "outcome_question": "Most people can be trusted",
    "income_var": "<column name used>",
    "income_buckets": ["Bottom 20%", "21-40%", "41-60%", "61-80%", "Top 20%"],
    "party_var": "<column name used>",
    "party_buckets": ["Democrat", "Independent", "Republican"],
    "n_total": <unweighted total>,
    "years_pooled": [1972, 1973, ...],
    "years_excluded": [...],
    "design": "Taylor-series linearization (svy package)"
  },
  "rows": [
    {
      "income": "Bottom 20%",
      "party": "Democrat",
      "value": 30.2,
      "ci_lower": 26.1,
      "ci_upper": 34.3,
      "standard_error": 2.1,
      "n_actual": 1234,
      "below_min_n": false
    }
  ]
}
```

**Before computing:** Print the column names you find in the parquet for trust/getahead/income/party/weight/strata/PSU so we can confirm the variables.

---

## After analysis: next steps in the rewrite

1. Push both JSON files to Tigris under `gss/derived/ch06-social-trust/` per our convention.
2. Read the proposal §6 (lines 2189–2287 of `~/Downloads/political_divide_gss.md`) to confirm the chapter structure.
3. Rewrite `article.mdx` around the proposal's structure: 6.1 Introduction → 6.2 Three trust measures (triangulation) → 6.3 Income-Trust Relationship (lead figure) → 6.4 Education, Income, and Trust (disentangling — uses the survey-analysis interaction) → 6.5 Meritocracy beliefs (uses the second interaction) → 6.6 Why partisan framing breaks down on this question (the catalog-data finding that contradicts the proposal's prediction).
4. Drop the Age angle from this chapter. The 7.5% under-35 finding is real but belongs in Ch. 11 (The Long View) or as a brief side-note in §6.6, not the lead.
5. Existing catalog data already on disk (12 datasets in `data/`): trust × {topline, age, education, party, race, income}, fair × {topline, income, party}, helpful × {topline, income, party}, getahead × {topline, income, party}.

## Inventory of catalog data already inspected (1972 → 2024 deltas)

- **Trust topline:** 46.6% → 24.3% (−22.2pp)
- **Fair topline:** 60.3% → 41.6% (−18.8pp)
- **Helpful topline:** 46.8% → 37.6% (−9.2pp)
- **Trust × Income (2024):** Bottom 20%=11.8, 21-40%=27.5, 41-60%=28.2, 61-80%=19.5(!), Top 20%=40.6 → 29-pt spread
- **Trust × Party (2024):** Dem 28.2, Rep 27.3, gap 0.9pp (effectively zero)
- **Fair × Party (2024):** Dem 44.3, Rep 45.3, gap −1.0pp
- **Helpful × Party (2024):** Dem 40.4, Rep 39.1, gap 1.3pp
- **Getahead × Party (2024):** Dem 58.4, Rep 68.2, Rep+9.8pp (gap *opened* over time)
- **Getahead × Income (2024):** Bottom 20%=76.3, Top 20%=54.9 → "low-income most pro-meritocracy" — INVERTED from trust gradient

## Proposal contradictions to surface in prose

- Proposal says "Partisan gap in social trust is moderate (~10 points) but growing" → actual data shows the gap has *closed* and slightly inverted on all three trust measures.
- Triangulation strengthens this finding (all three measures show ~zero partisan gap in 2024).
- This is exactly the kind of thing the proposal-as-guidance memory feedback applies to: surface the contradiction, don't paper over it.
