import { DropCap } from '@/components/Book/DropCap';
import { QuizBlock } from '@/components/Book/QuizBlock';
import { Figure } from '@/components/Book/Figure';
import { Callout } from '@/components/Book/Callout';
import { DataTable } from '@/components/Book/DataTable';
import { Pitfall } from '@/components/Book/Pitfall';
import { Takeaway } from '@/components/Book/Takeaway';
import { SoupRegionSmallMultiples, SoupElasticityScatter } from '@/components/Book/part2/SoupVisualEvidence';
import soupRaw from './data/soup-small-multiples.json';

export const soup = soupRaw;
export const coefficientRows = soupRaw.regionCoefficients.map((row) => ({
  region: row.group,
  estimate: row.estimate.toFixed(2),
  interval: `${row.ci_low.toFixed(2)} to ${row.ci_high.toFixed(2)}`,
  r2: row.r2.toFixed(2),
}));

<DropCap>

A national average is often the first honest summary and the first dangerous shortcut. It tells managers whether a pattern exists somewhere in the business. It does not tell them whether the pattern is broad enough to act on everywhere. Small multiples solve that problem by making the same comparison repeatedly, one market at a time.

</DropCap>

## The executive question: *is the national pattern broad-based or region-specific?*

The soup case has four census regions: East, Midwest, South, and West. A single national chart says Progresso raises price into a seasonal demand trough. The managerial question is whether that pattern is national or whether one region is doing most of the work.

Figure 1 uses small multiples with the same scales across regions. The same-scale choice matters. If every panel had its own y-axis, each region would look equally volatile. A common y-axis makes the differences in level and movement visible.

<Figure width="body-outset" caption="Figure 1. Progresso price rises into the weak-demand months in every region, but the share story is not equally strong everywhere. Small multiples reveal heterogeneity the national average hides.">
  <div className="not-prose">
    <SoupRegionSmallMultiples data={soup} />
  </div>
</Figure>

Figure 1 gives managers two lessons at once. First, the price seasonality is not a single-market artifact. Second, the share level differs sharply by region. The East is a much stronger Progresso market than the South or Midwest. A national pricing memo that ignores that fact would turn a visual average into an operational mistake.

## Small multiples as model preparation

Small multiples also prepare students for later regression. A regression with one coefficient asks for one summary relationship. A small-multiple chart asks whether that one summary is likely to be stable across groups.

Figure 2 previews the later pricing chapter by plotting log(Progresso volume) against log(Progresso price), separately by region. The downward trend line is an elasticity-style visual: a one percent increase in price is associated with a percent change in volume. We are not yet claiming a causal elasticity. We are teaching students how to see the slope before they see the equation.

<Figure width="body-outset" caption="Figure 2. The log price-volume slope is negative in every region, but the slope is steeper in the Midwest than in the East or West. This previews elasticity without yet making a causal claim.">
  <div className="not-prose">
    <SoupElasticityScatter data={soup} groupBy="region" />
  </div>
</Figure>

Figure 3 turns those visual slopes into coefficient intervals. This is a bridge view: still descriptive, but already moving toward statistical graphics.

<DataTable
  caption="Figure 3. Region-level log-log slopes differ enough that a national pricing story would be too coarse. These are month-adjusted descriptive previews, not final causal elasticity estimates."
  columns={[
    { key: 'region', label: 'Region', align: 'left' },
    { key: 'estimate', label: 'Slope', align: 'right' },
    { key: 'interval', label: '95% interval', align: 'right' },
    { key: 'r2', label: 'R-squared', align: 'right' },
  ]}
  rows={coefficientRows}
/>

Figure 3 should not replace Figure 2. The table makes the slopes precise; the scatterplots show whether the precision comes from a clean pattern or from compressing a messy one. Managers need both views before asking whether a national elasticity estimate is useful.

<Callout variant="caveat" title="Do not overread the slope">
  The slope in Figure 2 is an association in scanner data. It may reflect pricing strategy, promotions, seasonality, store mix, consumer demand, or omitted variables. Later chapters on pricing and identification will ask what variation makes an elasticity estimate credible.
</Callout>

<Pitfall title="Panels that aren't comparable">
Different y-axes make every region look equally important; different time windows make one market look stable and another volatile; different sorting makes the reader hunt for the pattern. **Hold the scale, the window, and the order fixed across panels so the only thing that varies is the data.**
</Pitfall>

<Pitfall title="Small multiples as decoration">
Four small charts are not automatically better than one. **Small multiples earn their space only when the managerial question is about heterogeneity** — regions, segments, products, cohorts, stores, or seasons.
</Pitfall>

<QuizBlock
  title="Concept check"
  intro="These three questions span the role of an explicit baseline as a comparison anchor, the same-scale rule that keeps small-multiple panels honest, and the judgment of when small multiples earn their space versus when a national average suffices."
  questions={[
    {
      prompt: <>A brand manager circulates a slide reading "Progresso non-winter price: 3.42 dollars" as evidence that pricing is too aggressive in the off-season. A colleague objects that the number, standing alone, cannot support that conclusion. What is the strongest version of the colleague's objection?</>,
      options: [
        { label: <>The figure should have been shown as an index relative to January rather than in dollars, because dollar levels are never the right unit for a pricing decision.</> },
        { label: <>A single number carries no meaning until it names what it is being compared with — the winter price, a competitor's price, or last year's — so "3.42" without a baseline anchor cannot say whether pricing is aggressive at all.</> },
        { label: <>The objection is misplaced: a single off-season average is sufficient, and adding any comparison would only clutter the slide.</> },
      ],
      correctIndex: 1,
      explanation: <>The article's core move is that every number on a chart is implicitly a comparison, and the comparison has to be named to mean anything. "3.42 dollars" is neither high nor low until it is set against an anchor — the winter price, a rival, or a prior year. Indexing is one way to express a comparison, but the claim that dollars are "never" the right unit overshoots; the real defect is the missing baseline, not the unit. Declaring one number sufficient is exactly the error the chapter warns against.</>,
    },
    {
      prompt: <>You build a four-panel small-multiple of soup volume by region. To "let each region breathe," a teammate sets each panel's y-axis to its own min and max so every region fills its frame. Why does the chapter treat this as a defect rather than a polish?</>,
      options: [
        { label: <>Independent y-axes make every region's swings look the same size, destroying the cross-panel comparison that is the whole point of small multiples; the axes must be shared so only the data varies.</> },
        { label: <>Independent y-axes are fine for volume but should never be used for price, so the issue is limited to this particular metric.</> },
        { label: <>The only problem is aesthetic: panels with different axes look mismatched, but the comparison itself remains valid.</> },
      ],
      correctIndex: 0,
      explanation: <>Small multiples work because the eye compares panels directly, and that only holds when the scale, the time window, and the panel order are fixed across every panel. Free y-axes rescale each region to fill its frame, so a tiny swing and a huge swing look identical — the reader reads shape and concludes the regions are alike when they are not. This is a correctness failure, not a cosmetic one, and it is not specific to volume versus price.</>,
    },
    {
      prompt: <>A VP asks whether the company's countercyclical pricing pattern "holds everywhere or just in a few markets." An analyst answers with the single national average price-by-month line. When does the national line suffice, and when do small multiples earn their space here?</>,
      options: [
        { label: <>The national line always suffices; small multiples are decorative and add cost without changing the decision.</> },
        { label: <>Small multiples always beat a national line, so every chart should be faceted by region regardless of the question.</> },
        { label: <>The national line answers "is there a pattern on average," but the VP's question is explicitly about heterogeneity across markets, so small multiples earn their space precisely because the question is whether the pattern survives region by region.</> },
      ],
      correctIndex: 2,
      explanation: <>The chapter's discipline is that small multiples are justified only when the managerial question is about heterogeneity — regions, segments, cohorts — and are otherwise clutter. A national average is the right tool for "is there a pattern overall," but the VP asked whether the pattern holds everywhere, which is a heterogeneity question by construction. Faceting earns its space here; faceting everything by reflex is the opposite error the chapter also warns against.</>,
    },
  ]}
/>

<Takeaway label="The call">

Use a national chart to find the headline. Use small multiples to decide whether the headline *survives contact with the business*. In the soup case, the countercyclical price pattern is broad, but the market strength differs by region. That is exactly the kind of distinction executives need before a pricing conversation becomes a policy.

</Takeaway>
