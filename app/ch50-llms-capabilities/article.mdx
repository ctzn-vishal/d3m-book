import { DropCap } from '@/components/Book/DropCap';
import { Figure } from '@/components/Book/Figure';
import { Callout } from '@/components/Book/Callout';
import { DataTable } from '@/components/Book/DataTable';
import { QuizBlock } from '@/components/Book/QuizBlock';
import { LlmCapabilityMap } from '@/components/Book/part5/RagVisionAgentDiagrams';

<DropCap>

A language model is not a chatbot that happens to answer questions. It is a language interface for workflows. The chatbot framing is what gets attention; the workflow framing is what generates business value. A manager who learns to see LLMs through the second frame can deploy them on the dozens of small language-shaped tasks the firm already pays people to do — summarize, classify, extract, translate, draft, answer, reason, narrate — and free those people to do the work the model can't.

</DropCap>

This article maps the capabilities a manager should expect from a modern LLM, the limits the model still has, and the operating discipline that comes with deploying language models as production components. The deep details of *how* to ask (prompting) and *how to integrate* (structured outputs) come in the next two articles. This one is about *what* to ask for.

---

## The Executive Question

> What are the discrete tasks a modern language model is actually good at — and where does it predictably fall short?

The honest version: LLMs are very good at language; they are unreliable at facts, math, current events, and any task that requires a database of truth they don't have. Most of the rest of this article maps that boundary.

---

## The Capability Map

A short, opinionated taxonomy. None of these are chatbot moves. They are *tasks*.

<Figure width="page-outset" caption="Figure 1. Eight capabilities every modern LLM supports. Each one is a task a manager would have given to an analyst; each is now available as an API call. The pattern is the same — language in, language out — but the business value is in the wiring.">
  <div className="not-prose">
    <LlmCapabilityMap />
  </div>
</Figure>

A walk-through of each, with one Bean &amp; Basket example:

- **Summarize.** Condense a long input. *"Summarize this 40-page supplier contract into a one-page brief covering parties, term, pricing, termination, and renewal."*
- **Classify.** Assign a label. *"Route this support ticket to billing, delivery, app, or quality."*
- **Extract.** Pull structured fields. *"From this sales call note, extract the customer name, the products discussed, and the next step."*
- **Translate.** Render across languages. *"Translate these French app reviews so the team can read them in English."*
- **Draft.** Generate text from instructions. *"Write a reply to this customer apologizing for the outage and offering a credit."*
- **Q&amp;A.** Answer using provided context. *"Using only the policy documents below, answer this employee's question about parental leave."* (This is RAG in §20.1.)
- **Reason / plan.** Work through a multi-step problem. *"Given these three retention experiments, recommend the next one to run."*
- **Narrate.** Explain a chart or a dataset. *"Describe what's notable in this monthly sales trend."*

The taxonomy is loose — many real tasks combine two or three of these. The point isn't to box every use case neatly; it's to give the manager vocabulary for the conversation about *what to use the model for*.

---

## The Hidden Strength: Translation Between Formats

A capability that doesn't get its own panel in the diagram but appears in nearly every production workflow: **format translation**.

- Free-text → structured JSON (§21.3).
- Bullet points → narrative prose.
- Spreadsheet rows → readable summaries.
- Legal language → plain English.
- Code → documentation.
- Email thread → CRM update.

A surprising amount of business labor is moving information between formats with minor enrichment. LLMs collapse that labor into a single API call.

---

## Where LLMs Are Unreliable

Symmetric clarity about limits:

<DataTable
  caption="Table 1. The standard LLM failure modes. Each has known mitigations, but none of the mitigations are 'use a bigger model'. They are workflow design choices."
  columns={[
    { key: 'limit', label: 'Limit', align: 'left' },
    { key: 'what', label: 'What happens', align: 'left' },
    { key: 'fix', label: 'Mitigation', align: 'left' },
  ]}
  rows={[
    {
      limit: 'Internal company facts',
      what: 'Model invents an answer or politely refuses.',
      fix: 'RAG (§20.1) — retrieve from the firm\'s indexed documents.',
    },
    {
      limit: 'Current events',
      what: 'Model\'s training data has a cutoff. It doesn\'t know about anything after.',
      fix: 'Web-search tool use, or live data ingestion via RAG.',
    },
    {
      limit: 'Exact arithmetic',
      what: 'Model can be confidently wrong on long-digit math.',
      fix: 'Call out to a calculator tool or code interpreter for arithmetic.',
    },
    {
      limit: 'Long-range consistency',
      what: 'Across many turns, the model may contradict earlier outputs.',
      fix: 'Structured memory + summarization between sessions.',
    },
    {
      limit: 'Reliability across runs',
      what: 'Same input can yield different outputs (temperature, sampling).',
      fix: 'Lower temperature, multi-run aggregation, schema validation.',
    },
    {
      limit: 'Hallucination on rare cases',
      what: 'For inputs poorly represented in training, the model can be confidently wrong.',
      fix: 'Confidence calibration + human-in-the-loop on edge cases.',
    },
    {
      limit: 'Bias',
      what: 'Outputs reflect biases in training data.',
      fix: 'Subgroup evaluation + targeted prompts to mitigate.',
    },
    {
      limit: 'Prompt injection',
      what: 'Retrieved or user-provided text contains instructions the model follows.',
      fix: 'Treat retrieved content as untrusted; system prompts that explicitly resist injection.',
    },
  ]}
/>

A recurring theme: the right response to most LLM weaknesses is a *workflow* fix, not a *model* fix. Wrap the model in RAG, give it tools, validate outputs against schemas, and route ambiguous cases to humans. These patterns occupy the rest of Chapter 21 and most of Chapter 22.

---

## Reasoning Models vs. Fast Models

A specific recent shift worth flagging — the rise of **reasoning models** (OpenAI's o-series, DeepSeek-R1, etc.) that explicitly "think" before answering. They are slower and more expensive than fast models. They are better at multi-step problems, math, planning, and code.

A practical rule:

- **Fast model** for tasks that are essentially pattern-matching — classification, extraction, summarization, drafting.
- **Reasoning model** for tasks that benefit from explicit deliberation — strategy questions, complex extraction with cross-references, math-heavy analysis.

The cost difference can be 5–20×. Use the reasoning model only where its extra thinking time actually changes the answer.

---

## The Model Landscape, Briefly

A note on what this article *doesn't* try to do: tell you which model to pick this week. The list of frontier providers (OpenAI, Anthropic, Google, Meta, Mistral, DeepSeek, Cohere, the open-weight ecosystem) and the rankings within it change every few months. Specific model names get stale faster than this book can be revised.

The stable claims:

- Multiple competitive providers exist; vendor lock-in is a real risk to manage.
- Open-weight models (Llama, Mistral, Qwen, Gemma, DeepSeek) have closed much of the gap to frontier closed models for many business tasks.
- Capability tier varies by task. A model that wins on summarization may not win on code generation.
- Smaller, specialized models often beat frontier models on narrow tasks — at a fraction of the cost.

The right operational posture is to abstract the model behind a single interface, evaluate per task on real data, and keep the door open to swapping providers as the landscape evolves.

<Callout variant="finding" title="The interface rule">
  Build the workflow such that the model is one swappable component. Do not tie the architecture to a specific provider's API. The provider that wins this quarter will not necessarily win next year.
</Callout>

---

## When LLMs Are the Right Tool

A short rubric for choosing whether to reach for an LLM:

- **Use an LLM when:** the input is language; the task is one of the eight capabilities above; the output needs human-readable language or schema-conforming structure; the cost of a small error is bounded.
- **Don't use an LLM when:** the task is exact computation (use code); the input is large structured data (use SQL); the task requires real-time guarantees the model can't make; the cost of any error is catastrophic and the system can't tolerate human review.

The most expensive failure mode is reaching for an LLM where a deterministic rule-based system would have worked better, faster, and more cheaply. The second most expensive is *not* reaching for an LLM where one would have collapsed a week of language-shaped manual work into a workflow.

---

## What Can Go Wrong

- **Treating the model as a search engine.** It's not — its training data has a cutoff and isn't your firm's. Pair with RAG.
- **Over-trusting outputs in language-shaped form.** Confident prose is not the same as correct prose.
- **Mismatched cost.** Using a frontier reasoning model for ticket classification when a small fast model would do the same job at 1/20 the cost.
- **Vendor lock-in.** Tying the architecture so tightly to one provider's quirks that switching becomes a quarter-long project.
- **Skipping evaluation.** Every LLM use case needs a golden set and a regular evaluation cadence. Without it, regressions ship silently.

---

## Managerial Takeaway

LLMs are language interfaces for workflows. The capabilities are language-shaped tasks an analyst would do; the limits are everything that requires a database of truth the model doesn't have. The right managerial posture is to see the firm's day as a portfolio of small language-shaped tasks and ask which ones the model can do better, faster, or cheaper.

> *Pick the capability. Pair the model with the right context (RAG) and the right tools (§21.4). Constrain the output (§21.3). Govern (§22).*

The next article — Prompting as Task Design — is about how to brief the model so it does the chosen task well.

<QuizBlock
  title="Concept check"
  intro="Three short questions on LLM capabilities."
  questions={[
    {
      prompt: <>A team wants an LLM to answer employee policy questions accurately. The internal HR handbook is not in the model's training data. The right architecture is:</>,
      options: [
        { label: <>Fine-tune the model on the handbook.</> },
        { label: <>RAG — index the handbook, retrieve at query time, ground the answer in retrieved chunks.</> },
        { label: <>Just ask the model and hope.</> },
      ],
      correctIndex: 1,
      explanation: <>The handbook changes; the model shouldn't be fine-tuned every time it does. RAG separates the firm's facts from the model's language ability.</>,
    },
    {
      prompt: <>A team uses a frontier reasoning model to route 100,000 tickets a day. Each routing decision is a simple language-pattern match. The most likely diagnosis is:</>,
      options: [
        { label: <>Optimal architecture.</> },
        { label: <>Mismatched cost — a small fast model would do the same job at a fraction of the price.</> },
        { label: <>The model is too small.</> },
      ],
      correctIndex: 1,
      explanation: <>Reasoning models are valuable for problems that need explicit deliberation. Routing is pattern-matching; a fast small model wins on cost without sacrificing accuracy.</>,
    },
    {
      prompt: <>The single most under-appreciated capability of LLMs in business is:</>,
      options: [
        { label: <>Chatbot conversation.</> },
        { label: <>Translation between formats — free text to structured JSON, prose to bullet points, legalese to plain English.</> },
        { label: <>Image generation.</> },
      ],
      correctIndex: 1,
      explanation: <>Format translation appears in dozens of workflows that don't look like "AI tasks" at first glance. It's where the biggest aggregate productivity gains live.</>,
    },
  ]}
/>
