import { DropCap } from '@/components/Book/DropCap';
import { Figure } from '@/components/Book/Figure';
import { Callout } from '@/components/Book/Callout';
import { DataTable } from '@/components/Book/DataTable';
import { QuizBlock } from '@/components/Book/QuizBlock';
import { AIWorkflowCard } from '@/components/Book/part5/MeasurementDiagrams';
import { CustomerVoiceStudioFlow } from '@/components/Book/part5/RagVisionAgentDiagrams';

<DropCap>

Part V started with a single observation: most of what a firm knows about its customers is not in a column. The chapters since then have built the toolkit for reading that knowledge — classical NLP for surface features, embeddings for semantic similarity, GPT measurement for constructs, RAG for grounded answers, vision and multimodal for non-text inputs, language models as workflow components, agents for action, and an evaluation rubric to govern the whole thing. This article wires all of it together into a single integrated workflow that lives next to the Customer Intelligence Studio from §17.4.

</DropCap>

The Customer Voice Intelligence Studio isn't a new method. It's the operational picture of every method in Part V running in one loop, on one company's customer evidence, under one governance card. The teaching point is not in the components — those have been covered. It's in the *connections*: how a sentiment signal flows into a construct measurement, how a measured construct feeds into a cluster, how a cluster triggers an agent action, how the agent's action gets monitored and revised.

---

## The Executive Question

> Can the firm run one integrated customer-voice loop — score, measure, cluster, retrieve, act, monitor — that produces the artefacts a sponsor needs to sign off on, and the cadence an operator needs to run it?

If the answer is no on any stage, that's where the next quarter's work goes.

---

## The Loop

The loop has six stages. Each stage produces an artefact that feeds the next.

<Figure width="screen-inset" caption="Figure 1. The Part V customer voice loop end to end. Six stages — classify, measure, cluster, retrieve, summarize-and-act, monitor — with a dashed feedback arrow that turns the loop into a system rather than a one-shot project.">
  <div className="not-prose">
    <CustomerVoiceStudioFlow />
  </div>
</Figure>

The stages, with the artefact each owes the next:

1.  **Classify.** Reviews and tickets get routed to topic buckets and tagged with urgency (§18.4). *Artefact:* per-document tags, a weekly volume-by-topic chart.
2.  **Measure.** Constructs are measured on every relevant document (§19.2): intent to return, severity, sense of betrayal, evasiveness, whatever the firm has named. *Artefact:* construct scores per document, weekly aggregate by region/product.
3.  **Cluster.** Documents are embedded (§19.1) and clustered (§19.1). New themes surface; emerging complaints get flagged. *Artefact:* clusters with names and example documents.
4.  **Retrieve.** When a question comes in — from an employee, from a manager, from a downstream agent — RAG (§20.1) pulls grounded context from the firm's knowledge base. *Artefact:* answers with citations.
5.  **Summarize and act.** A customer-insights agent (§21.3) reads the day's classified-and-measured documents, identifies actionable signals, drafts proposed responses, and routes them through a human-approval gate. *Artefact:* approved Slack alerts, drafted customer responses, populated CRM updates.
6.  **Monitor.** The §22.1 rubric runs continuously over the workflow's outputs. *Artefact:* the weekly evaluation dashboard, the rolling golden-set score, the alert log.

The feedback arrow is the part that turns this into a system: the monitor's findings reshape the classifier's labels, the construct definitions, the cluster boundaries, and the agent's prompt. Without that loop, the workflow ages out within a quarter.

---

## A Bean &amp; Basket Walk-Through

A concrete week in the life of the deployed studio.

**Monday morning.** The classifier processed last week's reviews and tickets. Volume is normal; ticket category distribution is stable; sentiment is +0.16 on average. The classified output flows into the measurement step.

**Monday afternoon.** Construct measurement runs. Most documents score modestly across all constructs. One cluster of reviews — about 80, mostly from the South-East — scores unusually high on "intent to switch" and "anger at perceived dishonesty". Pre-§19.2, the team would have seen these as "negative reviews" and routed them to a manager. The constructs name what is actually happening.

**Tuesday morning.** Embedding clustering surfaces those 80 reviews as a distinct cluster. The cluster is new — it didn't exist last month. A representative review is pulled for inspection: a complaint about a regional promotion that appears to have been mis-stated by a third-party affiliate.

**Tuesday afternoon.** RAG kicks in. The internal KB is searched for documentation about the affiliate program and the promotion language. The retrieved chunks reveal that the affiliate's claim was unauthorized and that there is an existing remediation playbook.

**Wednesday morning.** The customer-insights agent assembles the picture. It drafts a Slack alert to the South-East regional manager, a customer response template, a recommended affiliate-program audit, and a brief executive summary. All four sit in an approval queue.

**Wednesday afternoon.** A human reviewer approves the Slack alert verbatim, edits the customer response, approves the affiliate audit, and rejects the executive summary as premature. The agent's decisions are logged. The reviewer's adjustments feed back into the next iteration's prompt and training set.

**Thursday onward.** The acted-on alerts produce outcomes — a remediation email is sent, the affiliate's claim is corrected, the regional manager runs an in-store check. Those outcomes are tracked. The monitoring dashboard shows the cluster's volume declining over the next two weeks; the construct scores on related new reviews return to baseline.

That is the loop running. No single component is new in this article. The integration is.

---

## The Workflow Card for the Studio

Every shipped workflow needs the one-page contract. Restated here for the studio:

<Figure width="page-outset" caption="Figure 2. The Bean & Basket Customer Voice Intelligence workflow card. Every row corresponds to a question someone will ask about the system at some point — and every cell traces to an artefact produced by an earlier chapter in this part.">
  <div className="not-prose">
    <AIWorkflowCard />
  </div>
</Figure>

---

## The Executive Brief

A sponsor reviewing the studio should see a single page that traces every decision to an artefact. The structure:

<DataTable
  caption="Table 1. The one-page executive brief for the Customer Voice Intelligence Studio. Every section traces to an artefact already produced by an earlier chapter, and every cell can be drilled into."
  columns={[
    { key: 'section', label: 'Section', align: 'left' },
    { key: 'question', label: 'Question it answers', align: 'left' },
    { key: 'artefact', label: 'Source artefact', align: 'left' },
  ]}
  rows={[
    {
      section: 'Inputs',
      question: 'What sources are we listening to?',
      artefact: 'Data sources catalog (§18.1).',
    },
    {
      section: 'Surface signals',
      question: 'What can we see at the surface? (sentiment, topic shares, classification)',
      artefact: 'Classification + topic dashboard (§18.4, §18.5).',
    },
    {
      section: 'Constructs',
      question: 'What deeper signals are we measuring? (intent, severity, betrayal, evasiveness)',
      artefact: 'Construct measurement panel (§19.2).',
    },
    {
      section: 'Themes',
      question: 'What emerging clusters appeared this week?',
      artefact: 'Embedding clusters with names (§19.1).',
    },
    {
      section: 'Grounded answers',
      question: 'What can the team ask the knowledge base?',
      artefact: 'RAG Q&A interface with sources (§20.1).',
    },
    {
      section: 'Actions',
      question: 'What did the agent draft, and what did humans approve?',
      artefact: 'Agent log + approval queue (§21.3).',
    },
    {
      section: 'Evaluation',
      question: 'How well is the workflow performing on the eight dimensions?',
      artefact: 'AI evaluation dashboard (§22.1).',
    },
    {
      section: 'Governance',
      question: 'Who owns it, what are the failure modes, how is the workflow audited?',
      artefact: 'AI workflow card (§22.1, this article).',
    },
  ]}
/>

The brief is one page when each section is a paragraph and each artefact is a hyperlink. The page is also the syllabus for any new team member joining the workflow — read top to bottom and you know what the system does, why, and what to look at if something breaks.

---

## What This Studio Adds to the Part IV Loop

The Customer Intelligence Studio from §17.4 ran on structured features — RFM, engagement, demographics — and produced churn scores, segments, and targeting plans. The Customer Voice Intelligence Studio runs on *unstructured evidence* — reviews, tickets, social, KB documents — and produces topics, constructs, clusters, grounded answers, and acted-upon alerts.

The two studios are complementary:

- **The Part IV studio** knows *who is at risk and how to reach them*.
- **The Part V studio** knows *what they are saying and what to do about it*.

A mature firm runs both side by side. The customer who lands in the high-churn-risk decile *and* in the "anger about app reliability" cluster gets a different response than the customer in only one of the two. The Part IV score and the Part V cluster together tell the operator more than either alone.

<Callout variant="finding" title="The two-studio rule">
  Structured customer intelligence answers "who and how loud?" Unstructured customer intelligence answers "what and why?" Run both. The intersection is where the strongest actions live.
</Callout>

---

## Where Part V Ends and Part VI Begins

The chapter, and Part V, end where the work of Part VI begins. Part V built the methods. Part VI is about operating them as a portfolio:

- [§23.1 The Data Product View](/ch54-data-product-view) — treating every artefact as a product with an owner, a version, and a contract.
- [§24.1 Decision Memos](/ch55-decision-memos) — the one-page document that integrates every form of evidence into a recommendation.
- [§24.2 Monitoring, Feedback, and Learning Loops](/ch56-monitoring-feedback) — the portfolio-level operating cadence; how the two customer studios feed each other.
- [§25.1 The Final Integrative Case](/ch57-final-integrative-case) — a Bean &amp; Basket strategic decision that uses every Part of the book at once.

That's the operating-system view of decisions. The methods are the language. The system is the discipline.

---

## What Can Go Wrong

The studio inherits every failure mode of every method it uses. Some are worth restating because they get more dangerous in integration:

- **Confused construct definitions.** When §19.2 measurements feed §19.1 clusters that feed §21.3 agent actions, an ambiguous construct definition propagates all the way to a customer email.
- **Stale evaluation set.** The golden set was good a year ago. Customer language has shifted; the evaluation no longer represents production. Refresh quarterly.
- **Drift in the agent's tool surface.** A tool the agent depends on changes behavior (an API update, a schema change). The agent silently misuses it until someone reads the logs.
- **Lost ownership.** The team that built the studio rotated out. The card is six months stale. New team members can't reconstruct the design intent. Tie ownership to roles, not individuals.
- **Privacy creep.** The workflow that started as "look at app store reviews" gradually expanded to "look at app store reviews plus emails plus chat transcripts." Each addition needs a privacy review.

---

## Managerial Takeaway

The Customer Voice Intelligence Studio is the operational form of every method in Part V running in one loop, on one firm's customer evidence, under one governance card. The technology is no longer the bottleneck. The discipline of integration, evaluation, and governance is.

> *Wire the six stages. Ship the card. Run the eval. Approve through humans. Monitor weekly. Re-audit quarterly. Pair with the Part IV studio.*

The methods of Parts IV and V give the firm two new senses — *who* and *what*. Part VI is about turning those senses into a system that learns over time.

<QuizBlock
  title="Concept check"
  intro="Three short questions on the Customer Voice Intelligence Studio."
  questions={[
    {
      prompt: <>The Part IV Customer Intelligence Studio and the Part V Customer Voice Intelligence Studio together are best characterized as:</>,
      options: [
        { label: <>Redundant — pick one.</> },
        { label: <>Complementary — the structured studio answers "who and how loud", the unstructured studio answers "what and why"; the intersection is where the strongest customer actions live.</> },
        { label: <>The same workflow at different scales.</> },
      ],
      correctIndex: 1,
      explanation: <>Different evidence languages, different questions, complementary outputs. Mature customer-analytics organizations run both.</>,
    },
    {
      prompt: <>The dashed feedback arrow on the studio loop diagram represents:</>,
      options: [
        { label: <>An optional component.</> },
        { label: <>The monitoring step's outputs reshaping the classifier labels, construct definitions, cluster boundaries, and agent prompts on the next iteration — without it, the workflow ages out within a quarter.</> },
        { label: <>A retry mechanism.</> },
      ],
      correctIndex: 1,
      explanation: <>The feedback loop is what turns the workflow into a system rather than a one-shot deployment.</>,
    },
    {
      prompt: <>A new team member arrives three months after the studio ships. The fastest test of whether the workflow is operating well is:</>,
      options: [
        { label: <>Whether they can reproduce the model's accuracy.</> },
        { label: <>Whether they can read the workflow card and the executive brief, follow the artefacts to each stage, and explain the system to an outsider within an hour.</> },
        { label: <>Whether they can retrain the model.</> },
      ],
      correctIndex: 1,
      explanation: <>The system's durability is measured by how well its artefacts communicate to new readers. That's the studio test.</>,
    },
  ]}
/>
