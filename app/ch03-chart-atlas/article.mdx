import { DropCap } from '@/components/Book/DropCap';
import { Figure } from '@/components/Book/Figure';
import { DataTable } from '@/components/Book/DataTable';
import { Pitfall } from '@/components/Book/Pitfall';
import { Takeaway } from '@/components/Book/Takeaway';
import { ChartAtlas } from '@/components/Book/part2/ChartAtlas';
import atlasRaw from './data/chart-atlas.json';

export const atlas = atlasRaw;
export const sourceRows = [
  {
    source: 'Soup',
    scope: `{"${atlasRaw.metadata.soup_rows.toLocaleString()} store-months; $"}{atlasRaw.metadata.soup_stores.toLocaleString()} stores; {"${atlasRaw.metadata.soup_date_range}`,
    role: 'The Part II through-line for seasonality, pricing, volume, share, and skewed store-month behavior.',
  },
  {
    source: 'County',
    scope: `$"}{atlasRaw.metadata.county_rows.toLocaleString()} counties`,
    role: 'Cross-sectional evidence for distributions, scatterplots, demographic correlations, maps, intervals, and coefficient previews.',
  },
  {
    source: 'Zillow',
    scope: `{"${atlasRaw.metadata.zillow_states.toLocaleString()} state time series; $"}{atlasRaw.metadata.zillow_date_range}`,
    role: 'Long monthly housing series for lines, indexed comparisons, small multiples, and state-year heatmaps.',
  },
  {
    source: 'Teaching arrays',
    scope: 'Small illustrative totals',
    role: 'Used only for Pareto and waterfall charts, where the lesson is a business bridge rather than a natural field in the three cases.',
  },
];

<DropCap>

The chart atlas is not a gallery. It is a translation guide. Managers rarely need the name of every visualization technique, but they do need to know what visual comparison a business question is asking for. A histogram, a line chart, an indexed line, a heatmap, and a coefficient plot are not interchangeable styles. Each one answers a different managerial question and carries a different risk of overclaiming.

</DropCap>

## The executive question: *which chart form matches the managerial question?*

The atlas uses the real case data for this part of the book. Soup carries the Part II through-line. County data supplies cross-sectional and geographic examples. Zillow supplies long state time-series examples. The small teaching arrays appear only where the chart form is a managerial accounting bridge, not a natural field in the three datasets.

Figure 1 is the source contract. It matters because students should learn chart choice from evidence, not screenshots. Every miniature chart in Figure 2 is drawn from these sources and each card states a real finding or a deliberate teaching simplification.

<DataTable
  caption="Figure 1. The atlas is generated from the Part II case folder: soup for the pricing panel, county for cross-sectional and geographic evidence, and Zillow for long state time series."
  columns={[
    { key: 'source', label: 'Source', align: 'left' },
    { key: 'scope',  label: 'Scope',  align: 'left' },
    { key: 'role',   label: 'Role in the atlas', align: 'left' },
  ]}
  rows={sourceRows}
/>

## The atlas

Read Figure 2 from left to right inside each card:

1. The family names the evidence problem: distribution, comparison, time, relationship, geography, multivariate structure, uncertainty, or business bridge.
2. The miniature chart shows the visual form using the teaching data.
3. The finding states what this specific data example reveals.
4. The use, question, and trap lines say when the form should and should not be used.

<Figure width="screen-inset" caption="Figure 2. The chart atlas turns chart choice into managerial choice: each visual form is tied to a question, a real finding, and a misuse risk.">
  <div className="not-prose">
    <ChartAtlas data={atlas} />
  </div>
</Figure>

The important habit is to move from question to comparison to chart. If the question is whether Progresso store-month volume is skewed, use a distribution chart. If the question is which housing markets grew faster after 2020, use an indexed line or dot plot. If the question is whether demographic variables move together before a model, use a correlation matrix. The chart is the consequence of the comparison, not the starting point.

## How the atlas will be reused

Later chapters should refer back to this page instead of re-teaching chart basics. A pricing chapter can use the scatterplot and coefficient plot language to preview elasticity. A causal chapter can use interval plots and small multiples to explain effect heterogeneity. A machine-learning chapter can reuse heatmaps, lift charts, calibration views, and residual displays. An AI evaluation chapter can use the same uncertainty and dashboard discipline without pausing to define every chart from scratch.

The atlas also gives students a vocabulary for saying no. A map is useful only when location changes the decision. A density curve is useful when shape matters more than counts. A correlation matrix is useful for scanning many relationships, but it is a poor executive answer when the decision requires one ranked comparison. Simple charts are not inferior; mismatched charts are.

<Pitfall title="Starting from the tool">
"Make a map" or "make a dashboard" is not a business question. **Write the comparison first**, then let it select the chart.
</Pitfall>

<Pitfall title="The right chart at the wrong grain">
A county scatterplot cannot answer a household-level question, and a store-month histogram cannot by itself recommend a national price policy. **Match the grain of the chart to the grain of the decision.**
</Pitfall>

<Pitfall title="Analytical-looking charts overclaim">
Trend lines, coefficient plots, heatmaps, and intervals look authoritative on sight. **They still need units, denominators, definitions, uncertainty, and causal limits** before the manager can act on them.
</Pitfall>

The deeper failure is treating the atlas as a menu. It is better to treat it as a decision aid. The business question determines the comparison. The comparison determines the visual form. The visual form determines what the manager can see quickly and what must be stated as a limitation.

<Takeaway label="Decision rule">

When a manager asks for a chart, *translate the request into a comparison before opening the visualization tool*. Ask whether the problem is about distribution, comparison, time, relationship, geography, many variables, uncertainty, or a business bridge. Then choose the simplest chart that makes that comparison visible — and state what the chart cannot prove.

</Takeaway>
