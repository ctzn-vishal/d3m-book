import { DropCap } from '@/components/Book/DropCap';
import { Figure } from '@/components/Book/Figure';
import { Callout } from '@/components/Book/Callout';
import { DataTable } from '@/components/Book/DataTable';
import { Equation } from '@/components/Book/Equation';
import { M } from '@/components/Book/M';
import { QuizBlock } from '@/components/Book/QuizBlock';
import { WithinUnitVariation } from '@/components/Book/part3/ConceptDiagrams';
import { SoupRegressionLadder, PanelFixedEffectsVisual } from '@/components/Book/part3/CausalRegressionVisuals';
import part3 from '@/data/part3-cases.json';

export const data = part3;

<DropCap>

Across stores, customers, and markets, business units differ permanently in ways that are extremely hard to measure: neighborhood income, store layout, manager skill, local competitor density. If we pool data across units and run one regression, the differences *across* units pollute the relationship we wanted to estimate *within* units. The fix is structural: track each unit over time, and use that within-unit variation to identify the effect. The technique is called fixed effects, and it is the workhorse identification strategy of empirical business analysis.

</DropCap>

This article builds the within-unit-vs-across-unit distinction visually first, then derives the demeaning transformation that makes fixed effects mechanically equivalent to a simple regression on within-unit deviations, and ends with a data case on store fixed effects in soup pricing.

---

## The Executive Question: Are We Comparing Stores to Stores, or to Themselves?

A naive pooled regression of latte volume on price across 100 stores returns a *positive* slope: stores charging more sell more lattes. The recommendation looks like "raise prices."

The audit reveals the obvious story. Suburban stores charge $3.80 and sell 8,000 lattes a week; urban stores charge $3.00 and sell 5,000. The pooled comparison is *between* two different kinds of stores — and the across-store demographic gap dominates the within-store price response that any sane pricing decision would actually depend on.

The executive question is not "what is the correlation between price and volume in the dataset" but:

> *What happens to **the same store's** volume when **that store** changes its own price?*

That is the question fixed effects answers, by construction.

---

## Visualizing the Difference

Figure 1 shows the same conceptual picture three times. Three stores with different baseline levels (the intercepts) move modestly in response to a lever (the within-unit slopes). Looking *across* stores, the levels dominate the picture. Looking *within* each store, the slopes are similar and modest.

<Figure caption="Figure 1. Cross-unit comparison mixes stable level differences (intercepts) with the lever's effect. Store fixed effects subtract each store's own mean (dashed) and identify the slope from within-store wiggles only.">
  <WithinUnitVariation />
</Figure>

A pooled regression on this data fits one line through all the points and is dragged by the level gaps. A store-fixed-effects regression fits one slope to all the points *after subtracting each store's mean from each store's points* — and that slope is the within-store response.

| Comparison | What the variation comes from | What's absorbed | What's not |
| --- | --- | --- | --- |
| Pooled OLS | Across all stores and weeks | Nothing | Stable store differences + common time shocks |
| Entity (store) fixed effects | Within a store, across weeks | All stable store-level differences | Common time shocks (e.g. holidays) |
| Two-way fixed effects (store + week) | Within a store, against the common-week baseline | Stable store differences + week-level shocks | Time-varying, store-specific shocks |

The two-way fixed effects (TWFE) specification — store and week — is the standard starting point for any panel pricing analysis.

---

## The Method: Demeaning

Let units be indexed by <M>{String.raw`i \in \{1, \dots, N\}`}</M> and time periods by <M>{String.raw`t \in \{1, \dots, T\}`}</M>. The TWFE model is

<Equation
  label="Two-way fixed effects (TWFE)"
  latex={String.raw`Y_{it} = \beta_1 X_{it} + \beta_2 W_{it} + \alpha_i + \gamma_t + u_{it}`}
/>

where <M>{String.raw`\alpha_i`}</M> is an intercept for each unit, <M>{String.raw`\gamma_t`}</M> is an intercept for each time period, <M>{String.raw`X_{it}`}</M> is the treatment, and <M>{String.raw`W_{it}`}</M> are time-varying controls.

For thousands of units, estimating thousands of dummy intercepts is infeasible by brute force. The **within transformation** sidesteps the problem. Define each unit's mean over time:

<Equation
  latex={String.raw`\bar{Z}_i \;=\; \frac{1}{T}\sum_{t=1}^{T} Z_{it}`}
/>

and subtract it from every observation of that variable:

<Equation
  latex={String.raw`\tilde{Z}_{it} \;=\; Z_{it} - \bar{Z}_i`}
/>

Apply that transformation to both sides of the model. Because <M>{String.raw`\alpha_i`}</M> is constant over time within unit <M>{String.raw`i`}</M>, its mean is itself, and demeaning makes it vanish:

<Equation
  label="Demeaned regression"
  latex={String.raw`\tilde{Y}_{it} \;=\; \beta_1 \tilde{X}_{it} + \beta_2 \tilde{W}_{it} + \tilde{u}_{it}`}
/>

The coefficient <M>{String.raw`\beta_1`}</M> in this demeaned regression is *exactly* the fixed-effects coefficient from the original specification. Two consequences are worth memorizing:

1.  **Every time-invariant unit characteristic — observed or unobserved — has a demeaned value of exactly zero.** Square footage, ZIP-code income, store age, manager identity: all of it is absorbed. You do not have to measure stable confounders. You only have to assume they are stable.
2.  **All of the identifying variation comes from *changes within a unit*.** If a store never changes its price across the panel, that store contributes nothing to the price coefficient — its within-unit price variation is zero.

<Callout variant="definition" title="Strict exogeneity (the FE identifying assumption)">
  Fixed effects identify <M>{String.raw`\beta_1`}</M> causally if, after absorbing unit and time intercepts, the remaining within-unit variation in the treatment is unrelated to the residual demand shocks:
  <Equation latex={String.raw`\mathbb{E}\bigl[\, u_{it} \,\big|\, \tilde{X}_{i1}, \dots, \tilde{X}_{iT}, \alpha_i \,\bigr] = 0`} />
  In plain language: the timing of within-unit price changes must not be picking up on idiosyncratic local demand shocks. If a store cut prices the same week a local construction project blocked its drive-through, the FE design is broken even though the math runs.
</Callout>

---

## Centering, Live

The pooled-vs-centered comparison in Figure 2 lets you switch between the two views. The pooled scatter shows the misleading positive slope. The centered scatter slides each store's points to the origin and reveals the true within-store relationship.

<Figure width="screen-inset" caption="Figure 2. Pooled and centered views of the same panel data. In the pooled view the across-store demographic gap fits a positive slope; in the centered view, with each store's mean subtracted, the true within-store negative slope emerges.">
  <div className="not-prose">
    <PanelFixedEffectsVisual />
  </div>
</Figure>

The centered view is what the fixed-effects regression "sees." That visual lens — pull every unit's mean to the origin and look at the residual variation — is the most useful mental model for reading any fixed-effects coefficient.

<QuizBlock
  title="Concept check"
  intro="Four conceptual questions on fixed effects and within-unit variation. Click to reveal."
  questions={[
    {
      prompt: <>A pooled regression of volume on price returns a positive slope. The same regression with store fixed effects returns a negative slope. Which interpretation is most defensible?</>,
      options: [
        { label: <>The fixed-effects model is overfitting; the pooled model is more robust because it uses all the variation.</> },
        { label: <>Stable across-store differences (e.g. demographics) were inducing a positive correlation between price and volume across stores. Within stores — after absorbing those differences — price actually reduces volume.</> },
        { label: <>The sign flip means the dataset is corrupted.</> },
      ],
      correctIndex: 1,
      explanation: <>This is the canonical sign-flip story. The across-store comparison was contaminated by demographic confounding (high-end stores both charge more and sell more). Within stores, the demand response to a price change is negative — that is the slope the firm actually faces when it changes price.</>,
    },
    {
      prompt: <>Fixed effects automatically absorb which of the following?</>,
      options: [
        { label: <>Any unobserved factor that drives the outcome, whether or not it varies over time within a unit.</> },
        { label: <>Any unit-level factor that is constant over time, whether or not it was ever measured.</> },
        { label: <>Any factor that has a small variance.</> },
      ],
      correctIndex: 1,
      explanation: <>Unit fixed effects vanish under within-unit demeaning, taking with them every unit-level characteristic that does not move over time — measured or not. They do <em>not</em> control for things that vary inside the unit over time (local promotions, construction, weather). For those, you need time fixed effects or explicit time-varying controls.</>,
    },
    {
      prompt: <>A store never changed its price during the 24-week panel. In a store-fixed-effects regression of volume on price, this store…</>,
      options: [
        { label: <>Has a strong influence on the price coefficient because it has a large sample.</> },
        { label: <>Effectively contributes nothing to the price coefficient, because its within-store price variation is zero.</> },
        { label: <>Biases the price coefficient toward positive because of its constant pricing.</> },
      ],
      correctIndex: 1,
      explanation: <>FE identifies <M>{String.raw`\beta_1`}</M> from within-unit variation. A unit with no within-unit variation in the treatment carries no information about the treatment's effect — it is washed out by the demeaning. This is why FE estimates need adequate within-unit variation; a panel where most units never change the treatment will produce very imprecise estimates.</>,
    },
    {
      prompt: <>Which of the following is <em>not</em> absorbed by two-way (store + week) fixed effects?</>,
      options: [
        { label: <>Permanent neighborhood income differences across stores.</> },
        { label: <>A national holiday-week demand spike that hits every store the same way.</> },
        { label: <>A construction project in a single store's neighborhood that depresses traffic for three weeks.</> },
      ],
      correctIndex: 2,
      explanation: <>Store FE absorb stable store-level differences; week FE absorb shocks that hit all stores the same way. The construction project is <em>store-specific</em> and <em>time-varying</em> — exactly the gap TWFE leaves behind. If the timing of the price cut correlates with that construction, the FE design is biased. You would need a time-varying store control or an instrument.</>,
    },
  ]}
/>

---

## Data Case: Store Fixed Effects in the Progresso Panel

We climb the same regression ladder as Chapter 11.1 — bivariate, then with seasonality, then with competitor price, then with regional dummies, then with store fixed effects. The final rung is the within-store estimate that the previous article promised.

<Figure width="screen-inset" caption="Figure 3. The Progresso regression ladder, ending in store fixed effects. The store-FE specification explains a much larger share of variance because it absorbs the stable across-store differences. The coefficient on price settles near −2.23.">
  <div className="not-prose">
    <SoupRegressionLadder data={data.soup} />
  </div>
</Figure>

The step from Model 4 (regional dummies) to Model 5 (store fixed effects) is the smallest movement on the ladder *but the most meaningful from an identification standpoint*. Regional dummies absorb broad geographic differences. Store fixed effects absorb everything stable at the store level — neighborhood income, local competition, shelf placement, manager habits, parking. The coefficient moves modestly because regional dummies already captured a fair amount of the variation, but the *reason* we trust the estimate now is structural: there is no remaining stable store-level confounder it could absorb.

Reading the headline: a 1% increase in Progresso price within a given store, in a given month, is associated with roughly a 2.23% decrease in unit volume, after controlling for that store's stable characteristics, competitor pricing, and seasonal demand.

A pricing recommendation built on the naive −3.21 would have overstated price sensitivity by nearly a factor of one and a half, with predictable consequences — over-discounting to chase imagined volume, margin erosion, no actual lift. The fixed-effects estimate is the safe basis for pricing not because the number is smaller but because the comparison is fair.

---

## What Can Go Wrong

Fixed effects are powerful, not magical. Three traps:

1.  **Insufficient within-unit variation.** If most units never change the treatment, the demeaning leaves almost no signal, standard errors blow up, and the estimate is dominated by the few units that did. Always check the distribution of *within-unit* treatment variation before reading the coefficient.
2.  **Time-varying confounders.** Fixed effects absorb stable confounders only. A store that cut prices the week a local construction project blocked its parking lot will have a confounded within-store estimate, because the treatment timing tracks a time-varying shock the FE cannot absorb.
3.  **Lagged dependent variables in short panels (Nickell bias).** Including <M>{String.raw`Y_{i,t-1}`}</M> on the right-hand side violates strict exogeneity and creates a small-T bias that can be severe in panels under roughly ten periods. There are remedies (Arellano–Bond GMM and friends), but the default rule is: do not include a lagged dependent variable in a short FE panel without explicit treatment.

---

## Managerial Takeaway

Fixed effects are the cheapest, highest-value identification step a corporate analytics team has access to. Three rules:

- Default to two-way fixed effects (entity + time) for any panel pricing or rollout analysis. The cost is one line of code; the benefit is the absorption of every stable unit difference and every common time shock.
- Read the FE coefficient as a within-unit response. Always state the conditioning explicitly: "within a store, across weeks, holding ___ constant."
- After absorbing stable unit and time effects, name the time-varying, unit-specific shocks that could still confound the treatment. That residual list is the heart of a professional identification memo for a panel design.

The next chapter takes one further step: when fixed effects are not enough — when there is a specific event we want to evaluate — we move to designs that build the counterfactual from the comparison group's trajectory.
