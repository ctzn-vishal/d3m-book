import { DropCap } from '@/components/Book/DropCap';
import { Figure } from '@/components/Book/Figure';
import { Callout } from '@/components/Book/Callout';
import { Pitfall } from '@/components/Book/Pitfall';
import { Takeaway } from '@/components/Book/Takeaway';
import { Equation } from '@/components/Book/Equation';
import { M } from '@/components/Book/M';
import { QuizBlock } from '@/components/Book/QuizBlock';
import { MilkHeterogeneityFigure } from '@/components/Book/part3/CausalRegressionVisuals';
import part3 from '@/data/part3-cases.json';

export const data = part3;

<DropCap>

A causal study's headline is almost always a single number: the Average Treatment Effect (ATE). A push notification lifts conversion 3%. A loyalty offer raises sales 8%. These numbers are useful planning tools — and they are almost always a strategic illusion. The same 3% can hide a 15% lift among occasional shoppers, a 0% response from loyalists who would have bought anyway, and a small negative margin impact from deal-hunters who shifted from full-price purchases. The aggregate hides the targeting opportunity.

</DropCap>

This article works through **Heterogeneous Treatment Effects (HTE)** — what they are, how to estimate them, and why every causal study should report at least one heterogeneity analysis along with the headline ATE. We will distinguish two methods (subgroup analysis and interaction regression), name the traps that make naive heterogeneity claims false, and apply the framework to a real demographic targeting question.

---

## The Executive Question: For Whom Does This Action Work?

A coupon push generates a +3.0 pp aggregate lift in transaction probability. Before approving a blast to a million users, segment the response and the picture changes:

| Segment | Causal lift | Pre-coupon margin | Profitability of coupon |
| --- | --- | --- | --- |
| Brand loyalists (4+ visits/week) | 0 pp | $2.50 | Negative — pure margin erosion |
| Occasional shoppers (1/month) | +12 pp | $2.50 | Strongly positive |
| Deal-hunters (visit only on promotion) | +8 pp | $1.10 | Roughly breakeven |
| Premium connoisseurs (single-origin) | 0 pp | $4.50 | Negative — brand dilution |

The same lever is the right action for one segment and the wrong action for another. The blast is wasteful and possibly value-destroying because the loyalists alone — the largest single segment — absorb the entire margin from the genuine responders. Recognizing this is the difference between a campaign that loses money and one that makes it.

---

## Subgroup Analysis vs. Interaction Regression

Two operational ways to estimate heterogeneity. They are not interchangeable.

### Subgroup analysis

Split the sample by a pre-treatment characteristic (income tier, prior frequency, geography) and estimate the treatment effect separately within each subgroup.

The strengths: simple, easy to explain, makes no functional-form commitment about how the effect varies. The weaknesses: each subgroup uses a fraction of the sample, so standard errors blow up; and the differences between subgroups are not tested formally. Two segments with non-overlapping confidence intervals might still not be reliably different.

### Interaction regression

Estimate one regression that *includes* a segment indicator and its interaction with the treatment:

<Equation
  label="Interaction regression"
  latex={String.raw`Y_i \;=\; \beta_0 + \beta_1 \,T_i + \beta_2 \,D_i + \beta_3 \,(T_i \times D_i) + u_i`}
/>

where <M>{String.raw`T_i`}</M> is the treatment indicator and <M>{String.raw`D_i`}</M> is a binary segment indicator. The four coefficients each have a precise meaning:

- <M>{String.raw`\beta_0`}</M>: average outcome for the untreated, non-segment baseline.
- <M>{String.raw`\beta_1`}</M>: treatment effect in the **baseline group** (where <M>{String.raw`D_i = 0`}</M>).
- <M>{String.raw`\beta_2`}</M>: stable baseline difference between segments (regardless of treatment).
- <M>{String.raw`\beta_3`}</M>: **differential treatment effect** — how much the segment's response differs from the baseline group's.

The total treatment effect for the segment is

<Equation
  latex={String.raw`\tau_{\text{segment}} \;=\; \beta_1 + \beta_3`}
/>

and the formal test for "is the response different in this segment" is whether <M>{String.raw`\beta_3`}</M> is significantly different from zero. Interaction regression uses all of the data and produces a direct test of the *difference* between segments — both major improvements over simple subgroup analysis.

---

## Three Traps That Sink Naive HTE Claims

### Trap 1: The post-treatment segment

The single most common HTE error is defining a segment using a variable measured *after* the treatment. "Customers who clicked the email," "users who reached the upgrade page," "stores that hit $10k in monthly sales" — each of these is partly a *consequence* of the treatment. Conditioning on it produces collider bias from Chapter 11.2 and destroys the causal interpretation.

The rule is absolute: **segments must be defined on variables fixed before treatment was assigned.**

### Trap 2: Multiple testing (p-hacking)

If you slice the data by 20 different segmenting variables at the 5% significance level, you should *expect* roughly one to come up significant by chance alone, even when the treatment has no effect anywhere. Dashboards that let stakeholders slice lift by hundreds of customer features manufacture spurious heterogeneity at scale.

The defenses are well-known: pre-specify the segments of interest before looking at the data; apply a multiple-testing correction (Bonferroni, Benjamini–Hochberg) when many segments are tested; and treat exploratory subgroup findings as hypotheses to validate in a *fresh* experiment, not as conclusions in themselves.

### Trap 3: The noisy subgroup illusion

Slice the sample into "female shoppers aged 18–22 in Denver who buy decaf" and you get a large point estimate with an enormous standard error. Highly specific subgroups with eye-catching point estimates are almost always sampling noise. Always report the *interval* around segment effects, and refuse to act on a segment whose interval crosses zero.

<Callout variant="caveat" title="Pre-specify or perish">
  The credibility of a heterogeneity claim depends almost entirely on whether the segments were named <em>before</em> looking at the outcome. The same point estimate that is a useful finding when pre-specified is a likely false positive when discovered after slicing the data forty ways.
</Callout>

---

## Targeting with HTE

Once you have credible per-segment effects, targeting follows mechanically. Compute the per-segment expected profit:

<Equation
  latex={String.raw`\pi_{\text{segment}} \;=\; \tau_{\text{segment}} \times (\text{margin per response}) - (\text{cost per send})`}
/>

Send the treatment to segments where <M>{String.raw`\pi_{\text{segment}} > 0`}</M>; suppress it where <M>{String.raw`\pi_{\text{segment}} \le 0`}</M>. The headline aggregate ATE is rarely the decision-relevant number. The decision-relevant numbers are the per-segment <M>{String.raw`\tau_{\text{segment}}`}</M>s and the profitability they imply at each segment's margin.

A useful summary chart: the segment effect as a horizontal interval plot, sorted from most to least responsive. The visual lets a manager see at a glance which segments to target, which to ignore, and which to investigate further because the interval is too wide to act on yet.

<QuizBlock
  title="Concept check"
  intro="Three questions spanning the two-group counterfactual, single-unit synthetic controls, and segment-level effects."
  questions={[
    {
      prompt: <>The DiD estimator works by netting out two specific confounders. Which pair?</>,
      options: [
        { label: <>Measurement error in the treatment, and measurement error in the outcome.</> },
        { label: <>The stable baseline level difference between the treated and control groups, and any common time shock that affects both groups equally.</> },
        { label: <>Sampling noise in the pre period and sampling noise in the post period.</> },
      ],
      correctIndex: 1,
      explanation: <>DiD's two subtractions are exactly designed to remove (a) the time-invariant level gap between groups (treated vs. control), and (b) the time shock that hits both groups (post vs. pre). What is left is the part of the treated group's post change that the control group did not experience.</>,
    },
    {
      prompt: <>Why is the non-negativity constraint (<M>{String.raw`w_j \ge 0`}</M>) on donor weights so important for synthetic control?</>,
      options: [
        { label: <>Because negative weights cannot be computed efficiently.</> },
        { label: <>Because allowing negative weights would let the optimizer construct counterfactuals outside the convex hull of the donor pool — combinations that no real unit comes close to — and produce spurious pre-treatment fits by extrapolation.</> },
        { label: <>Because negative weights violate the parallel-trends assumption.</> },
      ],
      correctIndex: 1,
      explanation: <>Without the constraint, the optimizer could "short" a donor to chase a tighter pre-trend fit and produce a synthetic unit that is more extreme than any real one. The non-negative, sum-to-one constraints keep the counterfactual a convex combination of observed donor trajectories.</>,
    },
    {
      prompt: <>You estimate the treatment effect of an email campaign separately among "customers who opened the email" and "customers who did not open the email." Why is this analysis broken?</>,
      options: [
        { label: <>The two subgroups have different sample sizes, biasing the comparison.</> },
        { label: <>"Opened the email" is a post-treatment behavior — partly caused by the treatment itself. Conditioning on it induces collider bias and destroys the causal interpretation in both subgroups.</> },
        { label: <>Opening rates are too variable to support causal inference.</> },
      ],
      correctIndex: 1,
      explanation: <>The first rule of HTE is that segments must be defined on variables determined <em>before</em> treatment was assigned. Conditioning on a post-treatment behavior is a special case of collider bias and is not rescued by sample size.</>,
    },
  ]}
/>

---

## Data Case: Income Heterogeneity in the Milk Pricing Quasi-Experiment

The milk pricing study has a natural HTE question: does equal pricing nudge whole-milk share more in some neighborhoods than others? We stratify the supermarket sample by ZIP-code median income (low, medium-low, medium-high, high) and re-estimate the equal-pricing effect within each tier.

<Figure width="screen-inset" caption="Figure 1. Whole-milk share effect of equal pricing, by ZIP-code income tier. Each interval is the estimated lift in whole-milk share inside that income tier, with its 95% confidence interval. The effect is concentrated in lower-income ZIP codes and shrinks to near zero in higher-income ones.">
  <div className="not-prose">
    <MilkHeterogeneityFigure data={data.milk} />
  </div>
</Figure>

Two readings of Figure 1 are worth being explicit about. First, the *aggregate* effect from Chapter 10 (about +8 pp) averages across all four tiers, and is much smaller than the response in the lowest-income tier (+12.8 pp). The aggregate would have under-supported a targeted public-health intervention; the heterogeneity result over-supports it relative to the average. Second, the high-income tier's interval crosses zero — a defensible reading is "we cannot reject zero effect in this group with the data we have." That is not the same as "there is no effect there"; it is a precision statement about what this dataset can show.

For decision purposes, the figure does the targeting work directly. A policy aimed at moving whole-milk share would be much more cost-effective if it concentrated on lower-income ZIP codes, where the response is large and clearly distinguishable from zero. A blanket rollout across all neighborhoods would spend most of its cost producing little movement in the high-income tier.

---

<Pitfall title="P-hacking the segments">
Slicing by many segments without pre-specification or correction inflates false-positive rates dramatically — with 20 slices at the 5% level, one "significant" segment is expected by chance alone. **Pre-register the segments of interest, or apply a multiple-testing correction once the dashboard lets stakeholders slice freely.**
</Pitfall>

<Pitfall title="Post-treatment segmentation">
Segments defined on variables caused by the treatment — "customers who clicked," "users who reached the upgrade page" — are not segments; they are colliders, and conditioning on them destroys the causal interpretation. **Check that every segmenting variable was fixed before treatment was assigned.**
</Pitfall>

<Pitfall title="Cherry-picked aggregation">
Reporting only the segments with significant lifts and suppressing the rest is selection on the outcome. **Always show the full segment chart, including the null and negative findings.**
</Pitfall>

<Takeaway label="For the manager">

Heterogeneity is the bridge from causal inference to operational targeting. Three rules:

- Demand a segment chart along with every aggregate ATE. The aggregate is the planning number; the segment chart is the targeting number.
- Pre-specify the segments of interest before looking at the outcome. Post-hoc slices are hypotheses, not findings.
- Use the per-segment lift to compute per-segment expected profit, and let the suppression of unprofitable segments do as much economic work as the targeting of profitable ones.

The next chapter takes the targeting logic into the specific domain that depends on it most heavily: pricing.

</Takeaway>
